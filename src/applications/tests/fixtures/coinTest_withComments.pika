exec {
	const quarters :=  6.
	const dimes    :=  3.
	const nickels  :=  7.
	const pennies  := 17.
    
    # this is a single line comment
	const value := quarters * 25 +
    			 dimes    * 10 +
    			 nickels  *  5 +
    			 pennies.
	print value, _n_.
    
	const ncoins := quarters + dimes + nickels + pennies.
	# this should totally work#
	# this should work too#print ncoins, _n_.
    
	const moredimes := dimes > nickels.
	print moredimes, _n_.

	const boot := _true_.
	const boof := _false_.
    
	print boot; boof, _n_.
	print boot, boof, _n_.
}
